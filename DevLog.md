# 개발일지

## 2026-09-11 / 1일차

2D 핀볼 느낌을 만들고, 이미지에 스크립트를 붙여 조작하는 구조로 정리했다.

### 한 일
- 차지 발사 → 튕김 → 아래 점수 칸까지 기본 물리 동작을 확인했다.
- 런타임에 테이블을 그리던 하드코딩을 제거하고, 이미지에 역할 스크립트를 붙이는 방식으로 바꿨다.
- 위쪽 곡선은 `ArchCollider` 한 줄로 만들었고, 그림이 없을 때는 인게임 미리보기 선으로 확인했다.
- 발사를 연속 멀티볼로 바꿨다. 공은 20개 풀, 공끼리 충돌, 발사 세기 제한을 풀어 강하게 나가게 했다.
- UI 버튼 OnClick → `PinballManager.Fire` / `Restart`로 연결되게 했다.
- 한 라운드 20발. 득점하면 공은 회수되지만 다시 장전되지 않고, `0/20`이 되면 재시작 가능하다.
- 리스타트 시 점수는 초기화하고, 최고 점수는 `PlayerPrefs`에 저장·갱신한다.
- 발사 각도/속도/위치/회전을 조금씩 흔들리게 해서 같은 칸만 들어가는 문제를 줄였다.
- 내가 띄우던 임시 OnGUI 텍스트는 제거하고, 텍스트는 만든 UI만 매니저에 연결한다.

### 결정
- 장식 이미지는 스크립트 없음. 상호작용만 해당 컴포넌트를 붙인다.
- 아래 점수 칸에 들어가는 순간이 득점이자 그 공의 회수다. 낙구 전용 존은 두지 않는다.
- 아치는 MeshCollider가 아니라 2D `EdgeCollider` 한 줄이다.
- 클릭 감지는 버튼 UI가 하고, 매니저는 `Fire` / `Restart`만 제공한다.
- 남은 공·점수·최고점은 `TextMeshProUGUI`를 매니저에 연결해서 보여 준다.

### 내일
- 아치 그림과 테이블 이미지를 실제 아트에 맞춰 배치하기
- 발사 세기와 흔들림을 플레이하면서 수치 다듬기
